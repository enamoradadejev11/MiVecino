import { makeStyles } from "@material-ui/core/styles";

export const calificationStyles = makeStyles((theme) => ({
    gridContainerCalification: {
        paddingLeft: '70px',
        textAlign: 'left',
       },
}))